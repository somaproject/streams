#include "glconfig.h"

std::string LOGROOT("soma.openglconfig"); 

//
// Print configuration attributes.
//
void GLConfigUtil::examine_gl_attrib(const Glib::RefPtr<const Gdk::GL::Config>& glconfig)
{

//   log4cpp::Category& log = log4cpp::Category::getInstance(LOGROOT);

//   log.debugStream() << "\nOpenGL visual configurations :\n\n";

//   log.debugStream() << "glconfig->is_rgba() = "
// 		    << (glconfig->is_rgba() ? "true" : "false"); 
            
//   log.debugStream() << "glconfig->is_double_buffered() = "
// 		    << (glconfig->is_double_buffered() ? "true" : "false"); 
            
//   log.debugStream() << "glconfig->is_stereo() = "
// 		    << (glconfig->is_stereo() ? "true" : "false"); 
            
//   log.debugStream() << "glconfig->has_alpha() = "
// 		    << (glconfig->has_alpha() ? "true" : "false"); 
            
//   log.debugStream() << "glconfig->has_depth_buffer() = "
// 		    << (glconfig->has_depth_buffer() ? "true" : "false"); 
            
//   log.debugStream() << "glconfig->has_stencil_buffer() = "
// 		    << (glconfig->has_stencil_buffer() ? "true" : "false"); 
            
//   log.debugStream() << "glconfig->has_accum_buffer() = "
// 		    << (glconfig->has_accum_buffer() ? "true" : "false");
  

}


