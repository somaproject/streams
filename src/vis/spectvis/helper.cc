#include "helper.h"

namespace spectvis
{

std::list<int64_t> bin_range(timeid_t t1, timeid_t t2, timeid_t binsize)
{
  /* 
     Find the integer-labeled bins that are contained in the range [t1, t2]
     
     Note that this assumes that bin 0 is at [0, binsize-1]
     bin -1 is at [binsize, -1]
     
  */ 
  
}



}
